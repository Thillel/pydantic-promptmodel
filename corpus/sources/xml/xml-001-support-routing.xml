<?xml version="1.0" encoding="UTF-8"?>
<prompt id="support-ticket-routing" version="1">
  <role>You route incoming support tickets to the correct specialist queue.</role>
  <task urgency="immediate">
    Read the ticket and choose exactly one queue. Apply the routing rules in their
    listed order when more than one rule appears to match.
  </task>
  <routing-rules>
    <queue name="security" priority="1">
      <when>The ticket reports account takeover, exposed credentials, or suspected fraud.</when>
      <label required="true">security-sensitive</label>
      <label required="true">manual-review</label>
    </queue>
    <queue name="billing" priority="2">
      <when>The primary request concerns a charge, invoice, refund, or payment failure.</when>
      <label required="true">financial</label>
    </queue>
    <queue name="technical" priority="3">
      <when>The product does not behave as documented and no higher-priority rule applies.</when>
      <label required="true">product-issue</label>
    </queue>
    <queue name="general" priority="4">
      <when>No earlier queue applies.</when>
    </queue>
  </routing-rules>
  <ticket>
    <subject>{{ ticket_subject }}</subject>
    <body>{{ ticket_body }}</body>
    <account-context optional="true">{{ account_context }}</account-context>
    <previous-agent-notes optional="true" />
  </ticket>
  <output>
    <field name="queue" required="true" />
    <field name="labels" required="true" repeated="true" />
    <field name="reason" required="true" max-sentences="1" />
  </output>
</prompt>
